// Placeholder para desarrollo local (npm run dev / npm run build fuera de Docker).
// En Docker, docker-entrypoint.sh sobrescribe este archivo en cada arranque del
// contenedor a partir de docker/env.template.js + la variable de entorno API_BASE_URL.
window.__ENV__ = {};
